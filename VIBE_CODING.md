# Vibe Coding Notes

## Skill 标准结构（当前总结）

一个可用的 skill 通常由以下部分组成：

1. 必需文件
- `SKILL.md`
  - 定义 skill 的名称、描述、触发场景和执行流程。
  - 这是 skill 的核心入口说明。

2. 推荐文件
- `agents/openai.yaml`
  - 提供 UI 展示信息（例如显示名、简短描述）。
  - 不一定影响核心能力，但会影响可读性和使用体验。

3. 可选资源目录
- `scripts/`
  - 放可执行脚本，用于实现确定性的自动化动作。
  - 例如：按订单 ID 调 Shopify 查询的脚本。

- `references/`
  - 放参考文档（接口字段含义、业务规则、术语说明等）。
  - 用于在回答问题时补充稳定的语义依据。

- `assets/`
  - 放模板、静态素材或输出时要复用的资源。

## 它们如何协同工作

1. 先触发 skill
- 系统先根据 `SKILL.md` 的名称与描述判断是否命中该 skill。

2. 再执行流程
- 命中后读取 `SKILL.md` 正文，根据其中流程决定先做什么、后做什么。

3. 按需加载资源
- 需要执行动作时调用 `scripts/`。
- 需要解释语义时读取 `references/`。
- 需要模板或素材时使用 `assets/`。

---

后续可以持续在本文件追加：
- 新的 vibe coding 心得
- 常用 skill 设计模式
- 踩坑记录与最佳实践
